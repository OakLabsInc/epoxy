// not currently used...
// It might be a good idea to use Joi to validate some of the more complex
// documents we are sending around.
module.exports = [
  //{
    //typeName: 'String',
    //validation: (arg, assert) =>
      //assert(typeof arg === 'string'),
    //defaultArgs: ['email', 'password', 'sessionId', 'userId']
  //},
]
